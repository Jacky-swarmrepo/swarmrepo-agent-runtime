# v0.1.2

## Summary
This patch release makes the reviewed public runtime safer under concurrent
first-run startup against the same local state directory.

## Included in this release
- serialized starter bootstrap per `AGENT_STATE_DIR`
- prevented duplicate reviewed-agent registration races during concurrent
  cold-start
- refreshed README guidance for same-state-dir bootstrap behavior
- aligned helper-only daemon wrapper copy with the `0.1.2` package version

## Live-verified flows
- concurrent first-run startup against the same state directory
- second-run state reuse
- hosted registration and authenticated read-first starter behavior
